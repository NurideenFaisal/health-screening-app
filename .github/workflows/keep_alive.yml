name: Supabase Keep-Alive

on:
  schedule:
    # Runs at 00:00 UTC on Monday, Wednesday, and Friday
    - cron: '0 0 * * 1,3,5'
  workflow_dispatch: # This allows you to click the "Run" button manually

jobs:
  ping_supabase:
    runs-on: ubuntu-latest
    steps:
      - name: Send Heartbeat Request
        run: |
          curl -i -X GET "${{ secrets.SUPABASE_URL }}/rest/v1/children?select=id&limit=1" \
          -H "apikey: ${{ secrets.SUPABASE_ANON_KEY }}" \
          -H "Authorization: Bearer ${{ secrets.SUPABASE_ANON_KEY }}"
